<h1 align="center"> Users </h1>


<div class="center">
  <%= will_paginate %>
</div>
<%# @articles.each do |article| %>


<% @users.each do |user| %>
  <ul class="listing">
    <div class="row">
      <div class="well col-md-4 col-md-offset-4 center">
<!--        <li><%#= link_to gravatar_for(user), user_path(user) %></li>-->
        <li class="article-title">
          <%= link_to user.username, user_path(user) %> <%= "(Admin)" if user.admin? %>
        </li>
        <li><small><%= pluralize(user.novels.count,"novels") if user.novels %></small></li>
        <% if user_signed_in? and current_user.admin? %>
          <li><%=link_to "edit", edit_user_path(user) %></li>

          <li><%=link_to "delete", user_path(user),method: :delete,data: {confirm: "Are u sure ? "} %></li>
        <% end %>
      </div>
    </div>
  </ul>



  <!--  <div class="row">-->
  <!--    <div class="col-xs-8 col-xs-offset-2">-->
  <!--      <div class="well well-lg">-->
  <!--        <div class="article-title">-->
  <%#= link_to user.username, user_path(user) %>
  <!--        </div>-->
  <!--        <div class="article-body">-->
  <%#= truncate(user.email) %>
  <!--        </div>-->
  <!--        <div class="article-actions">-->
  <%#= link_to "Edit", edit_user_path(user), class: "btn btn-xs btn-primary" %>
  <%#= link_to "Delete", user_path(user), method: :delete, data: {confirm: "Are you sure ? "}, class: "btn btn-xs btn-danger" %>
  <!--        </div>-->
  <!--      </div>-->
  <!--    </div>-->
  <!--  </div>-->
<% end %>
<div class="center">
  <%= will_paginate %>
</div>







